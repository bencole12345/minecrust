syntax = "proto3";
package sbs5k_messages.block;

enum BlockType {
    EMPTY = 0;
    GRASS = 1;
    DIRT = 2;
    STONE = 3;

    // TODO: Water
    // TODO: Torch (might need separate ones according to which wall (north/south/east/west) they're attached)
}
